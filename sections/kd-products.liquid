<div class="kd-products-carousel-wrapper">
  {% if section.settings.is_loggedin %}
    logged in checked
  {% endif %}
</div>

{% schema %}
  {
    "name": "Kd Products Section",
    "settings": [
      {
        "type": "text",
        "id": "products_section_title",
        "label": "Title"
      }, {
        "type": "checkbox",
        "id": "is_loggedin",
        "label": "Only show for logged in users?"
      }
    ],
    "presets": [
      {
        "name": "Kd Products Section"
      }
    ]
  }
{% endschema %}